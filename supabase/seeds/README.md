# Database seeds

Files here are **not** run by `supabase db push`. Apply manually (staging/prod) when bootstrapping data such as the superuser role.

Do **not** move seed scripts into `supabase/migrations/` or they will auto-run on every environment.
